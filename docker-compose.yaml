version: '3.8'

services:
  db:
    image: mariadb:10.6 # ou a versão que preferir
    container_name: mariadb_db
    restart: always
    environment:
      MARIADB_ROOT_PASSWORD: 123
      MARIADB_DATABASE: ecommerce
      MARIADB_USER: everton
      MARIADB_PASSWORD: 123
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
volumes:
  db_data: